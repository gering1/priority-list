import React from 'react'

const EditButton = ({setCurrentlyEditing,id}) => {
    
   
    return (
        
        //show pencil icon to edit if not editing (edit index is -1), otherwise show checkmark (in process of editing)
        <div>
            
            
            
        </div>
    )
}

export default EditButton
